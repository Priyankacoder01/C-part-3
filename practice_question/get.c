#include <stdio.h>
int main ()
{
    char ch,b,c;
   char a[10];
    
    // ch = getch();
  gets(a);
    //b = getchar();
   // c = getche();
    // printf("ch = %c",ch);
     printf("a = %s",a);
    // printf("c = c",c);
    //printf("b = %c",b);
}